# Raw unzipped photo download - kept locally as the untouched source archive.
# The organized, web-ready copies live in public/images/products/.
Rusti-Shack-Images/

# Next.js website
node_modules/
.next/
out/
.vercel
npm-debug.log*
.DS_Store

# Secrets and local environment config - never commit real values.
# .env.example is intentionally kept (names/placeholders only).
.env*
!.env.example
